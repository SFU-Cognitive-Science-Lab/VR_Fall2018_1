using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PrintDatatoFile : MonoBehaviour {
    /*
    public ;
    private int correctCountCheck;
    private int incorrectCountCheck;

    // Use this for initialization
    void Start () {
        string path = "Assets/Resources/GameOutput.csv";
        StreamWriter writer = new StreamWriter(path, true);
    }
	
	// Update is called once per frame
	void Update () {

        string path = "Assets/Resources/GameOutput.csv";

        //Write some text to the test.txt file
        StreamWriter writer = new StreamWriter(path, true);
        writer.Write(Time.time + " , ");
        writer.Write(controller.transform.position.x + " , "); //writer.Write (" , ");
        writer.Write(controller.transform.position.y + " , "); //writer.Write (" , ");
        writer.Write(controller.transform.position.z + " , ");
        writer.Write(camStats.x_look + " , ");
        writer.Write(camStats.y_look + " , ");
        
    }
    */

    //WHEN YOU LEAVE FOR THE DAY, MAKE SURE THE EXPERIMENT IS PLAYABLE
    //I DON'T WANT TO HAVE TO COME IN AND DEBUG YOUR SCRIPTS

}
